# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - ./rbac.yaml
components:
  - ../base/
patches:
  - path: ./snapshotpolicy-patch.yaml
  - path: ./snapshotschedule-patch.yaml
